<!-- <mvc:View
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc">
   <Text text="Hello World From SAP UI5"/>
</mvc:View>

 -->
<!-- <mvc:View
   controllerName="ui5.walkthrough.controller.App"
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc">
    <Button
      text="{i18n>showHelloButtonText}"
      press=".onShowHello"/>
    <Input
      value="{Recipient>/recipient/name}"
      description="Hello {Recipient>/recipient/name}"
      valueLiveUpdate="true"
      width="60%"/>    
</mvc:View> -->


<mvc:View
   controllerName="ui5.walkthrough.controller.App"
   xmlns="sap.m"
   xmlns:mvc="sap.ui.core.mvc"
   displayBlock="true">
   <App>
    <pages>
     <Page title="{i18n>homePageTitle}">
      <content>
         <Panel
          headerText="{i18n>helloPanelTitle}">
          <content>
           <Button
            text="{i18n>showHelloButtonText}"
            press=".onShowHello"/>
           <Input
            value="{Recipient>/recipient/name}"
            description="Hello {Recipient>/recipient/name}"
            valueLiveUpdate="true"
            width="60%"/>
          </content>
         </Panel>
      </content>
     </Page>
    </pages>
   </App>
</mvc:View>